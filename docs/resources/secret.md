---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "durantic_secret Resource - durantic"
subcategory: ""
description: |-
  Manages a Durantic account secret — a named sensitive value stored encrypted. The secret value is never returned by the API after creation; Terraform retains the last written value in state.
---

# durantic_secret (Resource)

Manages a Durantic account secret — a named sensitive value stored encrypted. The secret value is never returned by the API after creation; Terraform retains the last written value in state.

## Example Usage

```terraform
# Copyright (c) HashiCorp, Inc.

# Minimal example — only required fields
resource "durantic_secret" "minimal" {
  name  = "my-secret"
  value = var.secret_value
}

# Full example with description
resource "durantic_secret" "example" {
  name        = "db-password"
  value       = var.db_password
  description = "Production database password"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the secret.
- `value` (String, Sensitive) Secret value. Sensitive — stored in state but never returned by the API.

### Optional

- `description` (String) Optional description of the secret.

### Read-Only

- `created_at` (String) Timestamp when the secret was created.
- `updated_at` (String) Timestamp when the secret was last updated.
- `uuid` (String) Unique identifier for the secret.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/bin/bash
# Copyright (c) HashiCorp, Inc.

# Note: the secret value is not returned by the API.
# After import, set the value attribute manually in your configuration.
terraform import durantic_secret.example <UUID>
```
