---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "durantic_machine_config Resource - durantic"
subcategory: ""
description: |-
  Manages desired configuration for an existing Durantic machine. Destroying this resource does not delete the machine.
---

# durantic_machine_config (Resource)

Manages desired configuration for an existing Durantic machine. Destroying this resource does not delete the machine.

## Example Usage

```terraform
# Copyright (c) HashiCorp, Inc.

variable "gateway_hostname" {
  type    = string
  default = "gateway-01"
}

data "durantic_machine" "gateway" {
  hostname = var.gateway_hostname
}

data "durantic_image" "rke2_server" {
  docker_image_url = "ghcr.io/durantic/linux-ubuntu-25.10:rke2-server-1.35"
}

resource "durantic_mesh_network" "cluster" {
  name         = "rke2-cluster-mesh"
  network_cidr = "10.50.0.0/24"
}

resource "durantic_variable" "gateway_public_ip" {
  name  = "DEMO_GATEWAY_PUBLIC_IP"
  value = data.durantic_machine.gateway.public_ip_addresses[0]
}

resource "durantic_machine_role" "gateway" {
  name          = "demo-gateway-haproxy-k8s"
  description   = "HAProxy gateway role for an RKE2 cluster"
  requires_mesh = true

  template_data = <<-EOT
    #cloud-config
    packages:
      - haproxy
  EOT
}

resource "durantic_machine_role" "rke2_server" {
  name          = "demo-rke2-server"
  description   = "RKE2 server role"
  image_uuid    = data.durantic_image.rke2_server.uuid
  requires_mesh = true

  template_data = <<-EOT
    #cloud-config
    runcmd:
      - echo "RKE2 server role applied"
  EOT
}

resource "durantic_machine_config" "gateway" {
  machine_uuid      = data.durantic_machine.gateway.uuid
  mesh_network_uuid = durantic_mesh_network.cluster.uuid

  role_names = [
    durantic_machine_role.gateway.name,
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `machine_uuid` (String) UUID of the existing machine to configure.

### Optional

- `auto_update` (Boolean) Whether automatic updates are enabled for this machine.
- `inject_agent` (Boolean) Whether agent injection is enabled for this machine.
- `kexec_installer` (Boolean) Whether kexec installer is enabled for this machine.
- `mesh_network_uuid` (String) UUID of the mesh network assigned to this machine.
- `role_names` (List of String) Complete list of role names assigned to this machine.
- `stun_enabled` (Boolean) Whether STUN is enabled for this machine.
- `target_disk` (String) Target disk configured for this machine.
- `tunnel_type` (String) Tunnel type configured for this machine.

### Read-Only

- `discovered_ip_addresses` (List of String) IP addresses discovered for this machine.
- `hostname` (String) Machine hostname.
- `is_online` (Boolean) Whether this machine is currently online.
- `mesh_ip_address` (String) Mesh IP address assigned to this machine.
- `needs_provisioning` (Boolean) Whether this machine needs provisioning.
- `pending_config_push` (Boolean) Whether this machine has a pending config push.
- `private_ip_addresses` (List of String) Private IP addresses for this machine. Contains the mesh IP when available.
- `public_ip_addresses` (List of String) Public/selectable IP addresses for this machine. Mirrors discovered IP addresses used by the Cluster Wizard.
- `uuid` (String) Unique identifier for the machine.
- `wg_ip_address` (String, Deprecated) Deprecated alias for `mesh_ip_address`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/bin/bash
# Copyright (c) HashiCorp, Inc.

terraform import durantic_machine_config.example <MACHINE_UUID>
```
